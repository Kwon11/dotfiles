-- lua looks for the files required under the "lua" directory automatically
require('vim_config') -- all vim settings should go in here
require('plugin') -- plugins bro, review them and make sure you're using what they provide
require('keys') -- stores keybinds
